<Window x:Class="ImageGenDotNet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageGenDotNet"
        mc:Ignorable="d"
        Title="AI Image Generator" Height="600" Width="1000" MinHeight="400" MinWidth="600">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Menu Bar -->
        <Menu Grid.Row="0" Background="#F8F8F8" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <MenuItem Header="_Tools">
                <MenuItem Header="_Run Integration Tests" Click="RunIntegrationTests_Click" 
                         InputGestureText="Ctrl+T">
                    <MenuItem.Icon>
                        <TextBlock Text="🧪" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Environment Status" 
                         Command="{Binding ShowEnvironmentStatusCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="🔧" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About" Click="About_Click">
                    <MenuItem.Icon>
                        <TextBlock Text="ℹ️" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>
        
        <!-- Top Control Panel -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="10" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Model Selection -->
                <Label Grid.Row="0" Grid.Column="0" Content="Model:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Medium"/>
                <ComboBox Grid.Row="0" Grid.Column="1" 
                         ItemsSource="{Binding AvailableModels}" 
                         SelectedItem="{Binding SelectedModel}"
                         Height="28" VerticalAlignment="Center" Margin="0,0,10,5"
                         Style="{DynamicResource ModernComboBoxStyle}"/>
                
                <!-- Prompt Input -->
                <Label Grid.Row="1" Grid.Column="0" Content="Prompt:" VerticalAlignment="Top" Margin="0,5,10,0" FontWeight="Medium"/>
                <TextBox Grid.Row="1" Grid.Column="1" 
                        Text="{Binding PromptText, UpdateSourceTrigger=PropertyChanged}"
                        Height="60" TextWrapping="Wrap" AcceptsReturn="True"
                        VerticalScrollBarVisibility="Auto" Margin="0,5,10,5"
                        Style="{DynamicResource ModernTextBoxStyle}"/>
                
                <!-- Control Buttons -->
                <StackPanel Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" Orientation="Vertical" VerticalAlignment="Center">
                    <Button Content="Generate" 
                           Command="{Binding GenerateImageCommand}"
                           IsEnabled="{Binding IsGenerating, Converter={StaticResource InverseBooleanConverter}}"
                           Style="{StaticResource ModernButtonStyle}"
                           Width="120" Height="32" Margin="0,0,0,6"/>
                    <Button Content="Enhance Prompt" 
                           Command="{Binding EnhancePromptCommand}"
                           IsEnabled="{Binding IsGenerating, Converter={StaticResource InverseBooleanConverter}}"
                           Style="{StaticResource SecondaryButtonStyle}"
                           Width="120" Height="28" Margin="0,0,0,3"/>
                    <Button Content="Enhance w/ Directions" 
                           Command="{Binding EnhancePromptWithDirectionsCommand}"
                           IsEnabled="{Binding IsGenerating, Converter={StaticResource InverseBooleanConverter}}"
                           Style="{StaticResource SecondaryButtonStyle}"
                           Width="120" Height="28" Margin="0,0,0,6"/>
                    <Button Content="Save Image" 
                           Command="{Binding SaveImageCommand}"
                           IsEnabled="{Binding GeneratedImage, Converter={StaticResource NullToBooleanConverter}}"
                           Style="{StaticResource SaveButtonStyle}"
                           Width="120" Height="28"/>
                </StackPanel>
                
                <!-- Options -->
                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,5,0,0">
                    <CheckBox Content="Auto Generate" 
                             IsChecked="{Binding AutoGenerate}" 
                             VerticalAlignment="Center" Margin="0,0,20,0"
                             Style="{DynamicResource ModernCheckBoxStyle}"/>
                    <CheckBox Content="Auto Generate After Enhance" 
                             IsChecked="{Binding AutoGenerateAfterEnhance}" 
                             VerticalAlignment="Center"
                             Style="{DynamicResource ModernCheckBoxStyle}"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Main Content Area -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Image Display Area -->
            <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Margin="10">
                <ScrollViewer x:Name="ImageScrollViewer" 
                             HorizontalScrollBarVisibility="Auto"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalAlignment="Stretch"
                             VerticalAlignment="Stretch"
                             Background="White"
                             PreviewMouseWheel="ImageScrollViewer_PreviewMouseWheel"
                             MouseMove="ImageScrollViewer_MouseMove"
                             MouseLeftButtonDown="ImageScrollViewer_MouseLeftButtonDown"
                             MouseLeftButtonUp="ImageScrollViewer_MouseLeftButtonUp"
                             MouseDoubleClick="ImageScrollViewer_MouseDoubleClick">
                    <Grid>
                        <!-- Generated Image -->
                        <Image x:Name="GeneratedImage" 
                              Source="{Binding GeneratedImage}"
                              Stretch="Uniform"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              RenderTransformOrigin="0.5,0.5">
                            <Image.RenderTransform>
                                <ScaleTransform x:Name="ImageScaleTransform" ScaleX="1" ScaleY="1"/>
                            </Image.RenderTransform>
                        </Image>
                        
                        <!-- Loading Indicator -->
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Visibility="{Binding IsGenerating, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <!-- Animated Loading Spinner -->
                            <Grid Width="60" Height="60" Margin="0,0,0,15">
                                <Ellipse Width="60" Height="60" Stroke="#3498db" StrokeThickness="6" 
                                        Opacity="0.3" Fill="Transparent"/>
                                <Ellipse Width="60" Height="60" Stroke="#3498db" StrokeThickness="6" 
                                        Fill="Transparent" StrokeDashArray="15,85" StrokeDashOffset="0"
                                        RenderTransformOrigin="0.5,0.5">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform x:Name="SpinnerRotation"/>
                                    </Ellipse.RenderTransform>
                                    <Ellipse.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetName="SpinnerRotation"
                                                                   Storyboard.TargetProperty="Angle"
                                                                   From="0" To="360" Duration="0:0:1"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Ellipse.Triggers>
                                </Ellipse>
                            </Grid>
                            
                            <!-- Progress Bar for additional feedback -->
                            <ProgressBar IsIndeterminate="True" Width="200" Height="8" Margin="0,0,0,10"
                                        Style="{DynamicResource ModernProgressBarStyle}"/>
                            
                            <!-- Dynamic Status Text -->
                            <TextBlock Text="{Binding CurrentOperationText}" HorizontalAlignment="Center" 
                                      FontStyle="Italic" FontSize="14" Foreground="#555" Margin="0,0,0,5"
                                      Visibility="{Binding CurrentOperationText, Converter={StaticResource StringToVisibilityConverter}}"/>
                            <TextBlock Text="{Binding DetailedStatusText}" HorizontalAlignment="Center" 
                                      FontSize="12" Foreground="#777"
                                      Visibility="{Binding DetailedStatusText, Converter={StaticResource StringToVisibilityConverter}}"/>
                        </StackPanel>
                        
                        <!-- Placeholder Text -->
                        <TextBlock Text="Generated images will appear here"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                  FontSize="16" Foreground="Gray" FontStyle="Italic"
                                  Visibility="{Binding GeneratedImage, Converter={StaticResource NullToVisibilityConverter}}"/>
                    </Grid>
                </ScrollViewer>
            </Border>
        </Grid>
        
        <!-- Enhanced Status Bar -->
        <StatusBar Grid.Row="3" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Height="28">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <!-- Activity Indicator -->
                    <Ellipse Width="8" Height="8" Margin="0,0,8,0"
                            Visibility="{Binding IsGenerating, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Ellipse.Fill>
                            <SolidColorBrush Color="#3498db"/>
                        </Ellipse.Fill>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                       From="1" To="0.3" Duration="0:0:0.8"
                                                       AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                    
                    <!-- Status Text -->
                    <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center"/>
                </StackPanel>
            </StatusBarItem>
            
            <!-- Auto-Generate Indicator -->
            <StatusBarItem>
                <StackPanel Orientation="Horizontal" 
                           Visibility="{Binding AutoGenerate, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="●" Foreground="#27ae60" Margin="10,0,3,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Auto-Gen" FontSize="11" Foreground="#27ae60" VerticalAlignment="Center"/>
                </StackPanel>
            </StatusBarItem>
            
            <!-- Environment Status Button -->
            <StatusBarItem HorizontalAlignment="Right" Margin="0,0,10,0">
                <Button Content="🔧 Config" 
                       Command="{Binding ShowEnvironmentStatusCommand}"
                       Style="{StaticResource StatusBarButtonStyle}"
                       ToolTip="Check environment configuration and API keys"
                       FontSize="10" Height="20" Padding="6,2"/>
            </StatusBarItem>
            
            <!-- Model Information -->
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding SelectedModel, StringFormat='Model: {0}'}" 
                          FontSize="11" Foreground="#666" VerticalAlignment="Center"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
    
    <!-- Window Resources for Converters and Styles -->
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <local:NullToBooleanConverter x:Key="NullToBooleanConverter"/>
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        
        <!-- Modern Button Style with Visual Feedback -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                               CornerRadius="4" 
                               BorderThickness="{TemplateBinding BorderThickness}"
                               BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980b9"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                           To="1.02" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                           To="1.02" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                           To="1" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                           To="1" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#21618c"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#bdc3c7"/>
                                <Setter Property="Foreground" Value="#7f8c8d"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#95a5a6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#7f8c8d"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#6c7b7d"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Save Button Style -->
        <Style x:Key="SaveButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#27ae60"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#229954"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1e8449"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Modern Progress Bar Style -->
        <Style x:Key="ModernProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Background" Value="#ecf0f1"/>
            <Setter Property="Foreground" Value="#3498db"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" 
                               CornerRadius="4">
                            <Grid>
                                <Border Name="PART_Track" 
                                       Background="{TemplateBinding Background}" 
                                       CornerRadius="4"/>
                                <Border Name="PART_Indicator" 
                                       Background="{TemplateBinding Foreground}" 
                                       CornerRadius="4"
                                       HorizontalAlignment="Left"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#bdc3c7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"
                                        VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#3498db"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#95a5a6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="BorderBrush" Value="#bdc3c7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                   BorderBrush="{TemplateBinding BorderBrush}"
                                   BorderThickness="{TemplateBinding BorderThickness}"
                                   CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" 
                                                    Content="{TemplateBinding SelectionBoxItem}"
                                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                    Margin="{TemplateBinding Padding}"
                                                    VerticalAlignment="Center"/>
                                    <ToggleButton Grid.Column="1" 
                                                Name="ToggleButton"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                Width="20">
                                        <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="#666" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup Name="Popup" 
                                  Placement="Bottom"
                                  IsOpen="{TemplateBinding IsDropDownOpen}"
                                  AllowsTransparency="True"
                                  Focusable="False"
                                  PopupAnimation="Slide">
                                <Border Background="White" BorderBrush="#bdc3c7" BorderThickness="1" CornerRadius="4"
                                       MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#3498db"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#95a5a6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern CheckBox Style -->
        <Style x:Key="ModernCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#2c3e50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="16" Height="16" 
                                   Background="White" 
                                   BorderBrush="#bdc3c7" 
                                   BorderThickness="1" 
                                   CornerRadius="3"
                                   Margin="0,0,6,0">
                                <Path x:Name="CheckMark"
                                     Data="M 2 6 L 6 10 L 14 2"
                                     Stroke="#27ae60"
                                     StrokeThickness="2"
                                     Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#3498db"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Status Bar Button Style -->
        <Style x:Key="StatusBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                               CornerRadius="3" 
                               BorderThickness="{TemplateBinding BorderThickness}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E8E8E8"/>
                                <Setter Property="BorderBrush" Value="#CCC"/>
                                <Setter Property="Foreground" Value="#333"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#DDD"/>
                                <Setter Property="BorderBrush" Value="#AAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
</Window>
